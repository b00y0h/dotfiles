#!/bin/bash

# Display manual setup steps reminder
# This runs once at the end

set -e

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✨ Setup Complete!"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "⚠️  Manual Setup Required (contains secrets):"
echo ""
echo "  1. NPM Authentication"
echo "     → npm login"
echo ""
echo "  2. GitHub CLI"
echo "     → gh auth login"
echo ""
echo "  3. AWS Credentials"
echo "     → aws configure sso"
echo ""
echo "  4. SSH Keys"
echo "     → Copy from 1Password/USB or generate new:"
echo "       ssh-keygen -t ed25519 -C \"your_email@example.com\""
echo "       gh ssh-key add ~/.ssh/id_ed25519.pub"
echo ""
echo "  5. 1Password CLI (if you use it)"
echo "     → op signin"
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "📚 Documentation"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "  Full guide: ~/DOTFILES_SYNC_GUIDE.md"
echo "  Quick ref:  ~/DOTFILES_QUICK_START.md"
echo ""
echo "  Useful commands:"
echo "    chezmoi update     - Pull latest dotfiles"
echo "    chezmoi edit FILE  - Edit and apply config"
echo "    chezmoi diff       - See what changed"
echo ""
echo "🎉 Your Mac is ready!"
echo ""
echo "Restart your terminal: exec zsh"
echo ""
